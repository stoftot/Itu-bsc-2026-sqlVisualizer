@using visualizer.Models
@rendermode InteractiveServer

<div class="toolbar">
    <div class="schema-part">
        <select class="sql-statements" @onchange="SelectChanged">
            @for (int i = 0; i < HomeState.Queries.Count; i++)
            {
                <option value="@i">@HomeState.Queries[i].Type</option>
            }
        </select>

        <button @onclick="RunQuery" class="text-button">
            <p class="text-button__text">Run Query</p>
            <i class="bi bi-play-fill"></i>
        </button>
    </div>
    
    <div class="editor-part">
        <div class="stepper">
            <button @onclick="StepPrevious"
                    disabled="@(HomeState.CurrentStepIndex <= 0)">
                <i class="bi bi-chevron-left"></i>
            </button>
            
            @for (int i = 0; i < HomeState.Steps.Count; i++)
            {
                @if (i == HomeState.CurrentStepIndex)
                {
                    <p class="stepper__step stepper__step--selected">@HomeState.Steps[i].Component.Keyword.ToSQLString()</p>
                }
                else
                {
                    <p class="stepper__step">@HomeState.Steps[i].Component.Keyword.ToSQLString()</p>
                }
            }
            <button @onclick="StepNext"
                    disabled="@(HomeState.CurrentStepIndex >= HomeState.Steps.Count - 1)">
                <i class="bi bi-chevron-right"></i>
            </button>
        </div>
        
        <div class="stepper animation-stepper">
            <button>
                <i class="bi bi-chevron-left"></i>
            </button>
            
            <button @onclick="HomeState.AnimatePlay" class="text-button">
                <p class="text-button__text">Animation</p> 
                <i class="bi bi-play-fill"></i>
            </button>
            
            <button>
                <i class="bi bi-chevron-right"></i>
            </button>
        </div>
        
        <div class="info-button">
            <InfoModal  />
        </div>
    </div>
</div>
