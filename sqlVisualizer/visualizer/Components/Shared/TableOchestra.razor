@inherits TableOchestraBase
@if (Tables != null)
{
    @foreach (var table in Tables)
    {
        <TableView @key="@Guid.NewGuid()"
            Table="table"/>
    }
}
else
{
    <p>Loading tables...</p>
}